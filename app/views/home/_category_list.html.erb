<div class="flex h-12 md:h-full w-full md:w-3/12 px-4 py-2 home-box" tabindex="2">
  <ul class="flex flex-row md:flex-col h-full w-full items-start justify-start gap-4 overflow-y-auto">
    <% @categories.each do |category| %>
      <li> <%= category.name %></li>
    <% end %>
  </ul>
</div>
